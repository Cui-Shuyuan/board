# embedding（确保 Qdrant 正在运行）

## WSL / Linux 推荐

```bash
# 启动 Qdrant（优先用 .tools 里的 Linux 原生 Qdrant，数据在 .tools/qdrant-data；没有时才回退 Windows exe）
./scripts/start_qdrant.sh

# 增量同步单个游戏索引（默认）
./scripts/rebuild_index.sh --game <game_name>

# 增量同步全部游戏索引（默认）
./scripts/rebuild_index.sh --all

# 强制全量重建（模型/提取逻辑大改时）
./scripts/rebuild_index.sh --game <game_name> --full
./scripts/rebuild_index.sh --all --full
```

> 如果直接跑 Python 版 `scripts/rebuild_index.py`：
> - Linux 原生 Qdrant 在 `localhost:6333`，通常不用额外设置；
> - 如果回退到了 Windows 版 Qdrant，则要设置：
>   `export QDRANT_URL=http://<Windows宿主IP>:6333`
>   WSL 里访问 Windows 上的 Qdrant 不能用 localhost，要用默认网关 IP（`ip route show default`）。

## Windows 旧命令

```bat
D:\Python\Python312\python.exe D:\workspace\board\scripts\rebuild_index.py --game <game_name>
D:\Python\Python312\python.exe D:\workspace\board\scripts\rebuild_index.py --all
D:\Python\Python312\python.exe D:\workspace\board\scripts\rebuild_index.py --all --full
```

# 启动服务

## WSL / Linux

```bash
# 启动 Qdrant（已运行会自动跳过）
./scripts/start_qdrant.sh

# 启动 API
./scripts/start_api.sh
```

## Windows 旧命令

```bat
D:\qdrant\qdrant.exe
cd /d D:\workspace\board\backend\BoardAI.Api
dotnet run
```

或者 `dotnet run --rebuild-index <game_name>`
