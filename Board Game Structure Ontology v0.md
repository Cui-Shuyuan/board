# Board Game Structure Ontology v0.1

## 设计目标

本体用于描述现代桌面游戏中的静态结构概念。

本体不描述具体游戏规则。

本体只定义：

* 存在什么
* 它们具有什么性质
* 它们之间有什么关系

具体规则由游戏实例定义。

---

# Level 0: Primitive

以下概念没有类型，为不可再分解的基础概念。

## Object（对象）

定义：

可被识别、引用或操作的实体或抽象概念。

性质：

* 可被命名
* 可被引用
* 可拥有属性

---

## Zone（区域）

定义：

游戏中的抽象逻辑容器，表示一组物件的空间归属关系。

性质：

* 可包含对象
* 可为空
* 可具有公开或隐藏属性
* 不依赖版图和实体，只由规则决定

---

## State（状态）

定义：

某个时刻游戏世界中为真的断言。

性质：

* 可被改变
* 可被检查
* 描述对象“此刻怎样”，而非“是什么”

---

## Property（属性）

定义：

对象或区域固有的、由游戏规则预先定义的特征。

性质：

* 在对象进入游戏时即确定（除非特定效果修改它）
* 描述对象"是什么"，而非"此刻怎样"

---

# Level 1: Game Concepts


## Player（玩家）

类型：

Object

定义：

直接参与游戏并能够做出决策的实体。

性质：

* 拥有资源
* 拥有分数
* 持有部分游戏对象

---

## Resource（资源）

类型：

Object

定义：

一种可被拥有、计数和转移的价值对象。

性质：

* 可被持有
* 可被计数
* 可被转移

---

## Score（分数）

类型：

Object

定义：

用于衡量玩家表现并参与胜负判定的对象。

性质：

* 可被计数
* 可被比较

---

## Piece（物件）

类型：Object

定义：承载独立信息的游戏对象，每一个具有可区分的身份。

性质：
* 拥有属性
* 承载规则或状态信息
* 各个 Piece 之间不可互换
* 通常可移动、可放置、可被持有
* 通常可公开或隐藏

---

## Aid（辅助物）

类型：Object

定义：用于向玩家展示信息、辅助玩家识别和操作的游戏实体，不参与游戏逻辑运作。

性质：
* 不参与游戏规则的运作，仅服务于玩家的信息获取
* 熟练玩家在规则允许的前提下可以不依赖辅助物进行游戏
* 不产生、不修改、不删除游戏逻辑中的任何元素

---

## Token（指示物）

类型：

Object

定义：

用于表示资源、状态、计数或归属关系的对象。

性质：

* 无独立信息，同一类型的所有 Token 可互相替代
* 可放置、可移动、可被持有
* 通常数量有限，表示资源的 Token 也可能无数量限制

---

## Reserve（储备区）

类型：Zone

定义：存放可按照游戏规则获取的对象的区域。

性质：
* 包含可供玩家获取的对象
* 对象被获取后通常离开该区域
* 对象被作为费用支付后通常回到该区域

---

## Discard Pile（弃牌堆）

类型：Reserve

定义：存放已使用、被弃置或已移除的 Piece 的储备区域。

性质：
* 对象通常为公开可见（Public）
* 通常有顺序，且仅允许将对象放置于顶部

---

## Player Zone（玩家区域）

类型：Zone

定义：与单个 Player 绑定的专用 Zone ，用于存放该玩家所拥有、控制或专属访问的 Object 。

性质：

* 与一个且仅一个 Player 关联（通过 Ownership 状态绑定）
* 作为玩家个人游戏状态的物理或逻辑边界
* 可见性由具体子类定义
* 通常限制可容纳的对象类型或数量上限
* 对象进入该区域后，其归属关系通常确立为该区域的关联玩家（除非规则特殊说明）

---

## Ownership（归属关系）

类型：State

定义：表示某个 Zone 或 Object 归属于哪个 Player 。

性质：
* 可作用于 Zone
* 可作用于 Object
* 指向的目标是一个 Player
* 可在游戏过程中被改变

---

## Cost（费用）

类型： Property

定义： 指明一个效果在被触发前必须被履行的代价。

性质：

* Effect 的内部组成部分之一
* 不满足则该效果不可触发

---

## Content（内容）

类型：Property

定义：效果属性中描述结算时应当发生的具体事件的部分。

性质：

* Effect 的内部组成部分之一
* 由一个或多个待执行的事件描述组成
* 在 Resolve 阶段被实例化为真实事件

---

## Effect（效果）

类型： Property

定义：指明在满足特定条件时将要发生的代价与结果的完整规格。

组成：
* Cost
* Content

性质：
* Cost 和 Content 可以有一个为空，但不可以同时为空

---

## Information Visibility（信息可见性）

类型：Property

定义：决定游戏对象或区域的信息可以被哪些玩家观察，以及这种观察是强制的还是可选的规则。

基本状态：

- Public（公开）：必须公开给所有玩家观察
- Private（私有）：仅拥有者可以观察，不可以公开给其他玩家
- Protected（保护）：拥有者可以选择是否向其他玩家临时展示
- Partially Public（部分公开）：对象或区域中只有部分内容对所有玩家公开
- Hidden（隐藏）：所有玩家均不可观察

性质：
* 可作用于对象
* 可作用于区域
* 可在游戏过程中被改变

---

# Level 2: 

## Card（卡牌）

类型：

Piece

定义：

一种 Piece 。通常扁平矩形，有正反两面

---

## Tile（板块）

类型：

Piece

定义：

一种 Piece 。通常较小且形状不定

---

## Supply（供应堆）

类型：Reserve

定义：存放无差别的、可直接取用的对象的区域。是公共供应堆与玩家供应堆的基类。

性质：
* 对象通常完全相同，每个对象可互相替代
* 获取方式为直接取用，不需要抽取或随机
* 玩家对"取多少"有控制力，对"取哪个"无需控制
* 对象数量可能有限或无限
* 不支持顺序操作
* 不支持洗混

## Public Supply（公共供应堆）

类型：Supply

定义：所有玩家均可直接取用的 Supply。

性质：
* 任何玩家都可以从中拿取
* 不存在专属访问权

## Player Supply（玩家供应堆）

类型：Supply

定义：按玩家划分的私有区域，用于存放暂时不属于任何玩家、但只有特定玩家才能从中拿取的对象。

性质：
* 只供被分配到的玩家取用，其他玩家不能作为合法操作目标
* 其中的对象通常尚未被该玩家「获得」
* 对象可能被预先划拨到玩家面前，等待被消耗、安装或转移

---

## Market（市场）

类型：Reserve

定义：存放可见的、各不相同的对象，玩家可主动选择获取的区域。

性质：
* 对象通常各不相同，每个对象有独立身份或效果
* 所有对象对玩家可见
* 玩家主动选择获取哪一个，对"取哪个"有完全控制力
* 无顺序关系，对象之间不构成序列
* 获取后通常按规则补充新对象

---

## Deck（牌堆）

类型：Reserve

定义：存放有序叠放的对象序列，从固定位置抽取的区域。

性质：
* 对象有固定顺序
* 顺序在洗混后确定
* 每次抽取从固定位置（顶部）取走对象
* 支持洗混（随机重排顺序）

---

## Pool（池）

类型：Reserve

定义：存放不完全相同的对象的无序集合，获取哪一个由随机方式决定。

性质：
* 对象通常各不相同，每个对象有独立身份或效果
* 每次抽取为独立随机事件，无记忆性
* 不支持顺序操作
* 抽取后对象身份才公开
* 支持概率调整（加入或移除特定对象改变后续抽取概率）

---

## Player Holding（玩家保留区）

类型：Player Zone

定义：由单个玩家专属拥有的、用于存放该玩家已获得但尚未激活（未进入公共游戏逻辑）的 Piece 或 Token 的私有逻辑容器。

性质：
* 区域内的对象通常未被触发效果，不直接参与公共规则的交互（如计分、战斗、生产）
* 不强制要求对象必须可被打出（部分 Token 可能永久存放于此，仅作为终局计分凭证）
* 常用于存放“已保留的卡牌”、“秘密任务指示物”、“未建造的板块”或“个人目标标记”

---

## Development Area（发展区）

类型：Player Zone

定义：玩家已放置（通常为已购买、已打出、已建造）并公开可见的 Piece 的专属区域。

性质：

* 信息可见性通常为 Public
* 对象一旦进入该区域，通常不可撤销
* 该区域构成玩家“场上存在”的主要部分，是其他玩家判断对手实力的重要依据

---

## Public Board（公共版图）

类型：Aid

定义：用于直观呈现游戏全局或局部 Zone 布局的大型物理参照物。

性质：
* 标识一个或多个 Zone 的位置、边界和内容
* 物理形态不固定，也可由多个部分拼接而成
* 不产生 Zone，Zone 由游戏规则独立定义

---

## Player Board（玩家面板）

类型：Aid

定义：分配给单个玩家的专属物理参照物，用于标识该玩家私有 Zone 的布局。

性质：
* 通常每个玩家各一块
* 标识该玩家专属的 Zone 及其内容
* 物理形态不固定
* 不产生 Zone，Zone 由游戏规则独立定义

---

## Player Aid（玩家提示卡）

类型：Aid

定义：记录规则摘要、流程参考或计分信息的便携物理参照物。

性质：
* 通常为卡片或薄板形式
* 内容为游戏规则的浓缩或索引
* 可有正反两面

---

## Rulebook（规则书）

类型：Aid

定义：完整记录游戏规则的文档，供玩家在游戏前学习或游戏中查阅参考。

性质：
* 记录游戏的完整规则、流程和组件说明
* 通常在游戏开始前阅读学习，游戏中可作为参考
* 不参与游戏逻辑的运作

# Level 3:

## Hand（手牌）

类型：Player Holding

定义：存放Card的一种玩家保留区。

性质：
* 信息可见性通常为 Private（仅拥有者可观察）或 Protected（拥有者可选择展示）

--- 