# 已退役：这批 batch*.py 是 2026-09 一次性把 92 条 cue 铺出来时用的**临时脚手架**

用户 2026-09-20 定调：「脚本就应该完全由你自己写，不应该让程序去生成（过账除外）……
教学动画是一次做好永久使用的东西。」所以：

- **不要再运行这些脚本**（它们会整份重写 `anim/full.json`，把后来手写的改动冲掉 ✗）；
- 保留它们只为查"当年这一批是怎么铺的"（每个文件头部都写清楚了那一批的范围与理由）；
- 现在改动画 = **直接改 `games/splendor/tutorial/anim/full.json`**，见同目录上层的 `anim/README.md`。
