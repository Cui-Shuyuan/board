---
name: tutorial-module
description: 第五阶段讲规模块——技术选型定稿（Unity 原生安卓客户端 + 数据驱动程序化动画）与待定事项
metadata:
  type: project
---

# 讲规模块（第五阶段 Tutorial Tree）

## 需求（2026-08-14 用户口述）

播放被切成小段的**静态音频 + 动画**，让客人自助学会规则：

- 设备：店内提供的平板，白名单连接内网 wifi，与店内电脑通信。**内网不向客人开放**（安全 + 兼容性考虑）
- 播放器控制：手动下一条 / 自动播放 / 拖动进度条 / 上下滑动选章节跳转
- **打断闭环**：客人随时打断提问 → 问答模块登场，prompt 带当前章节信息 → 回答后可选继续提问或继续学习；**继续学习 = 从当前章节重播**（章节切得细，重播优于续播——用户例：「拿取三 m[打断][回答]ei 颜色不同的宝石」体验很差）
- 用户对动画制作和 Unity 都零基础

## 技术选型（2026-08-15 讨论定稿）

### 客户端：Unity 原生安卓 App

- **安卓平板**（非 H5）。测试机：**一加 Ace 2（主测试机，骁龙 8+ Gen 1）+ 红米 K30 Pro（兼容性下限）**
- 「C# 后端方便接 Unity」的理由在**原生客户端**前提下成立：前后端共享 C# 模型类/序列化格式
- H5 被否的关键理由：**浏览器麦克风要求 HTTPS 安全上下文**——内网 HTTP 的 H5 做不了按住说话（未来 PTT 核心交互）；原生 App 无此限制。Unity 原生还能一个引擎包掉动画/音频/聊天 UI
- 内容仍走数据驱动：tutorial.json + 音频 + 动画资源从店内电脑 HTTP 拉取，App 壳基本不更新
- iPad 不在考虑范围（安卓侧载无痛）

### 动画：数据驱动 + 程序化生成（用户委托 Claude 制作）

**动画 = 数据不是美术文件**——`tutorial.json` 里定义时间轴步骤（哪个素材图、什么变换、时长、字幕），Unity 播放器用 C# 代码执行：

- **零第三方依赖**（不用 Rive/Lottie/插件），纯 C# 协程/插值——用户不碰 Unity 编辑器也能完成，代码全是文本
- **素材现成**：`games/civolution/media/` 的组件裁切图就是演员（卡牌、板块、骰子、八角柱）
- **Unity 场景零搭建**：空场景 + 启动脚本，整个播放器 UI 由代码构建
- **能力边界**：程序化动画（移动/翻转/缩放/高亮/淡入淡出/字幕/时序）✅ 全可做；美术级动画（逐帧插画/3D/粒子/审美调优）❌ 不能做
- **迭代工作流**：Claude 写数据+代码 → 用户真机预览 → **用户文字描述问题**（当前模型不支持读图，看不了截图）→ Claude 调参。用户切到支持读图的模型后循环会快很多

### 后端

- 新增 `TutorialService` 读 tutorial.json + 资源目录，暴露章节树接口
- 打断时 `ChatOrchestrator` 的 system prompt 注入章节上下文（章节名 + keywords + concept_refs——问答命中率会高）

## 待定事项

- `tutorial.json` 完整数据格式（章节树 + 音频 + 动画时间轴 + 字幕 + 打断上下文）——下一步先定这个
- 音频来源：TTS 生成（快、批量、机械）vs 真人录音（贵、慢、质感好）
- 最小原型范围：一个章节跑通「播放→打断→问答→重播」全链路

## 相关记忆

- [[project-overview]] — 开发阶段规划（第五阶段）
- [[interaction-model]] / [[runtime-architecture]] — 问答模块现状与打断衔接点
- [[user-preferences]] — 数据驱动哲学、程序确定性

**Why:** 第五阶段的技术选型经历了 H5→原生的大转向（麦克风安全上下文是关键推手），新会话需要知道已定稿的结论和 Claude 承担动画制作的角色边界。
**How to apply:** 讲规模块的任何实现讨论以「Unity 原生 + 数据驱动程序化动画」为前提；动画迭代依赖用户文字反馈。
